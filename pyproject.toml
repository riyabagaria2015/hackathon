[project]
name = "hackathon"
version = "0.1.0"
description = "Hackathon: Days 1–5 — asyncio, Ollama+Pydantic, Gradio streaming, Director table read, puppet theater."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "edge-tts>=7",
    "gradio>=6",
    "ollama>=0.6.2",
    "pydantic>=2.13.4",
]
