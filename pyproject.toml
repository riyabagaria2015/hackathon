[project]
name = "hackathon"
version = "0.1.0"
description = "Hackathon: Days 1–4 — asyncio, Ollama+Pydantic, Gradio streaming, Director table read."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=6",
    "ollama>=0.6.2",
    "pydantic>=2.13.4",
]
